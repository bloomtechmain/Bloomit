## Rules
- Always present a plan before modifying or creating files
- Wait for explicit approval ("go ahead", "yes", "do it") before executing
- Never skip the planning step
- Always update `backend/src/databasse.sql` to reflect any schema changes (new tables, new columns, dropped columns) so it stays the source of truth for future tenant provisioning